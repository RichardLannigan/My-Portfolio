using UnityEngine;
using System.Collections;

public class Obi : CharacterBase
{
    // Use this for initialization
    void Start()
    {
        renderer.material.color = new Color(0.2f, 0.2f, 1);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
